---
title: "Hello, world"
description: "The first entry. What this space is for, and why it exists."
date: 2026-01-01
tags: ["meta"]
draft: true
---

This is a placeholder post so the Blogs section has something to render. Replace or delete it once you've written your first real entry — add new posts by dropping a `.md` file into `src/content/blog/`, with frontmatter matching the shape here (`title`, `description`, `date`, optional `tags`, and `draft`).

Posts marked `draft: true` are excluded from the listing and build. Set it to `false` (or remove it) to publish.
