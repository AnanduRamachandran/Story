// @ts-check
import { defineConfig } from 'astro/config';

// Custom domain via GitHub Pages (see /public/CNAME).
// Update this if the final domain differs from the placeholder below.
export default defineConfig({
  site: 'https://ananduramachandran.com',
});
